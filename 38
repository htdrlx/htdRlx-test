local Library = loadstring(game:HttpGet("https://raw.githubusercontent.com/htdrlx/htdRlx-test/refs/heads/main/newUI.lua.txt"))()

local Window = Library:CreateWindow({
    Title = "Banana Cat Hub",
    Subtitle = ": Blox Fruit",
    Image = "rbxassetid://128185233852701"
})

local ShopTab = Window:AddTab("Shop")
local StatusTab = Window:AddTab("Status And Server")
local LocalTab = Window:AddTab("LocalPlayer")
local SkillTab = Window:AddTab("Hold And Select Skill")
local FarmTab = Window:AddTab("Farming")
local StackTab = Window:AddTab("Stack Farming")
local FruitTab = Window:AddTab("Fruit And Raid Dungeon")
local SeaTab = Window:AddTab("Sea Event")
local RaceTab = Window:AddTab("Upgrade Race")
local ItemTab = Window:AddTab("Get And Upgrade Item")
local VolcanoTab = Window:AddTab("Volcano Event")
local ESPTab = Window:AddTab("ESP")
local PVPTab = Window:AddTab("PVP")
local WEBHOOKTab = Window:AddTab("WEBHOOK")
local SETTINGTab = Window:AddTab("SETTING")

local ShopGroup = ShopTab:AddLeftGroupbox("Shop")
local FightingGroup = ShopTab:AddLeftGroupbox("Fighting Shop")
local AbilityGroup = ShopTab:AddLeftGroupbox("Ability Shop")

local StatusGroup = StatusTab:AddLeftGroupbox("Player Status")
local ServerGroup = StatusTab:AddLeftGroupbox("Server")

local LocalGroup = LocalTab:AddLeftGroupbox("Player Mods")
local SkillGroup = SkillTab:AddLeftGroupbox("Skills")
local FarmGroup = FarmTab:AddLeftGroupbox("Auto Farm")
local StackGroup = StackTab:AddLeftGroupbox("Mob Stack")

local FruitGroup = FruitTab:AddLeftGroupbox("Fruit")
local RaidGroup = FruitTab:AddLeftGroupbox("Raid")

local SeaGroup = SeaTab:AddLeftGroupbox("Sea Features")
local RaceGroup = RaceTab:AddLeftGroupbox("Race Upgrade")
local ItemGroup = ItemTab:AddLeftGroupbox("Items")
local VolcanoGroup = VolcanoTab:AddLeftGroupbox("Volcano")
local ESPGroup = ESPTab:AddLeftGroupbox("ESP Features")
local PVPGroup = PVPTab:AddLeftGroupbox("PVP Features")

local WebhookGroup = WEBHOOKTab:AddLeftGroupbox("Webhook")
local SettingGroup = SETTINGTab:AddLeftGroupbox("Settings")

ShopGroup:AddButton({
    Text = "Redeem Code",
    Func = function()
        print("Redeem Code")
    end
})

ShopGroup:AddButton({
    Text = "Teleport Old World",
    Func = function()
        print("Teleport Old World")
    end
})

ShopGroup:AddButton({
    Text = "Teleport Third Sea",
    Func = function()
        print("Teleport Third Sea")
    end
})

ShopGroup:AddButton({
    Text = "Buy Dual Flintlock",
    Func = function()
        print("Buy Dual Flintlock")
    end
})

ShopGroup:AddButton({
    Text = "Reroll Race",
    Func = function()
        print("Reroll Race")
    end
})

ShopGroup:AddButton({
    Text = "Buy Ghoul Race",
    Func = function()
        print("Buy Ghoul Race")
    end
})

ShopGroup:AddButton({
    Text = "Buy Cyborg Race",
    Func = function()
        print("Buy Cyborg Race")
    end
})

FightingGroup:AddToggle("BlackLeg", {
    Text = "Black Leg",
    Default = false,
    Callback = function(Value)
        print("Black Leg:", Value)
    end
})

FightingGroup:AddToggle("FishmanKarate", {
    Text = "Fishman Karate",
    Default = false,
    Callback = function(Value)
        print("Fishman Karate:", Value)
    end
})

FightingGroup:AddToggle("Electro", {
    Text = "Electro",
    Default = false,
    Callback = function(Value)
        print("Electro:", Value)
    end
})

FightingGroup:AddToggle("SuperHuman", {
    Text = "Super Human",
    Default = false,
    Callback = function(Value)
        print("Super Human:", Value)
    end
})

FightingGroup:AddToggle("DeathStep", {
    Text = "Death Step",
    Default = false,
    Callback = function(Value)
        print("Death Step:", Value)
    end
})

FightingGroup:AddToggle("SharkmanKarate", {
    Text = "Sharkman Karate",
    Default = false,
    Callback = function(Value)
        print("Sharkman Karate:", Value)
    end
})

FightingGroup:AddToggle("ElectricClaw", {
    Text = "Electric Claw",
    Default = false,
    Callback = function(Value)
        print("Electric Claw:", Value)
    end
})

FightingGroup:AddToggle("Godhuman", {
    Text = "Godhuman",
    Default = false,
    Callback = function(Value)
        print("Godhuman:", Value)
    end
})

FightingGroup:AddToggle("SanguineArt", {
    Text = "Sanguine Art",
    Default = false,
    Callback = function(Value)
        print("Sanguine Art:", Value)
    end
})

AbilityGroup:AddButton({
    Text = "Skyjump [$10,000 Beli]",
    Func = function()
        print("Buy Skyjump")
    end
})

AbilityGroup:AddButton({
    Text = "Buso Haki [$25,000 Beli]",
    Func = function()
        print("Buy Buso Haki")
    end
})

AbilityGroup:AddButton({
    Text = "Observation Haki [$750,000 Beli]",
    Func = function()
        print("Buy Observation Haki")
    end
})

AbilityGroup:AddButton({
    Text = "Soru [$100,000 Beli]",
    Func = function()
        print("Buy Soru")
    end
})

StatusGroup:AddButton({
    Text = "Refresh Status",
    Func = function()
        print("Refresh Status")
    end
})

ServerGroup:AddButton({
    Text = "Copy Server JobId",
    Func = function()
        print("Copy Server JobId")
    end
})

LocalGroup:AddToggle("AutoBounty", {
    Text = "Auto Bounty",
    Default = false,
    Callback = function(Value)
        print("Auto Bounty:", Value)
    end
})

LocalGroup:AddToggle("AntiAFK", {
    Text = "Anti AFK",
    Default = false,
    Callback = function(Value)
        print("Anti AFK:", Value)
    end
})

SkillGroup:AddToggle("HoldSkill", {
    Text = "Hold Skill",
    Default = false,
    Callback = function(Value)
        print("Hold Skill:", Value)
    end
})

SkillGroup:AddButton({
    Text = "Select Skill Slot",
    Func = function()
        print("Select Skill Slot")
    end
})

FarmGroup:AddToggle("AutoFarm", {
    Text = "Auto Farm",
    Default = false,
    Callback = function(Value)
        print("Auto Farm:", Value)
    end
})

FarmGroup:AddToggle("KillAura", {
    Text = "Kill Aura",
    Default = false,
    Callback = function(Value)
        print("Kill Aura:", Value)
    end
})

StackGroup:AddToggle("MobStack", {
    Text = "Mob Stack",
    Default = false,
    Callback = function(Value)
        print("Mob Stack:", Value)
    end
})

StackGroup:AddToggle("BringMob", {
    Text = "Bring Mob",
    Default = false,
    Callback = function(Value)
        print("Bring Mob:", Value)
    end
})

FruitGroup:AddToggle("AutoFruit", {
    Text = "Auto Fruit",
    Default = false,
    Callback = function(Value)
        print("Auto Fruit:", Value)
    end
})

FruitGroup:AddToggle("FruitKaitun", {
    Text = "Fruit Kaitun",
    Default = false,
    Callback = function(Value)
        print("Fruit Kaitun:", Value)
    end
})

RaidGroup:AddToggle("AutoRaid", {
    Text = "Auto Raid",
    Default = false,
    Callback = function(Value)
        print("Auto Raid:", Value)
    end
})

SeaGroup:AddToggle("SeaEvent", {
    Text = "Sea Event",
    Default = false,
    Callback = function(Value)
        print("Sea Event:", Value)
    end
})

SeaGroup:AddToggle("SeaBeast", {
    Text = "Sea Beast",
    Default = false,
    Callback = function(Value)
        print("Sea Beast:", Value)
    end
})

RaceGroup:AddButton({
    Text = "Upgrade Race",
    Func = function()
        print("Upgrade Race")
    end
})

RaceGroup:AddButton({
    Text = "Reroll Race",
    Func = function()
        print("Reroll Race")
    end
})

ItemGroup:AddToggle("AutoSword", {
    Text = "Auto Sword",
    Default = false,
    Callback = function(Value)
        print("Auto Sword:", Value)
    end
})

ItemGroup:AddToggle("AutoMaterial", {
    Text = "Auto Material",
    Default = false,
    Callback = function(Value)
        print("Auto Material:", Value)
    end
})

VolcanoGroup:AddToggle("VolcanoEvent", {
    Text = "Volcano Event",
    Default = false,
    Callback = function(Value)
        print("Volcano Event:", Value)
    end
})

ESPGroup:AddToggle("PlayerESP", {
    Text = "Player ESP",
    Default = false,
    Callback = function(Value)
        print("Player ESP:", Value)
    end
})

ESPGroup:AddToggle("ChestESP", {
    Text = "Chest ESP",
    Default = false,
    Callback = function(Value)
        print("Chest ESP:", Value)
    end
})

PVPGroup:AddToggle("AttackAura", {
    Text = "Attack Aura",
    Default = false,
    Callback = function(Value)
        print("Aura:", Value)
    end
})

WebhookGroup:AddToggle("WebhookEnable", {
    Text = "Enable Webhook",
    Default = false,
    Callback = function(Value)
        print("Webhook Enable:", Value)
    end
})

WebhookGroup:AddButton({
    Text = "Test Webhook",
    Func = function()
        print("Test Webhook")
    end
})

WebhookGroup:AddButton({
    Text = "Copy Webhook URL",
    Func = function()
        print("Copy Webhook URL")
    end
})

SettingGroup:AddToggle("AutoSave", {
    Text = "Auto Save Config",
    Default = true,
    Callback = function(Value)
        print("Auto Save:", Value)
    end
})

SettingGroup:AddToggle("LowCpu", {
    Text = "Low CPU Mode",
    Default = false,
    Callback = function(Value)
        print("Low CPU Mode:", Value)
    end
})

SettingGroup:AddButton({
    Text = "Load Config",
    Func = function()
        print("Load Config")
    end
})

SettingGroup:AddButton({
    Text = "Save Config",
    Func = function()
        print("Save Config")
    end
})

SettingGroup:AddButton({
    Text = "Reset UI",
    Func = function()
        print("Reset UI")
    end
})

Library:Notify({
    Title = "Banana Cat Hub Loaded",
    Description = "Hub Loaded Successfully",
    Duration = 5
})
