local function UpdateText(obj)
    if (obj:IsA("TextLabel") or obj:IsA("TextButton")) and type(obj.Text) == "string" then
        obj.Text = obj.Text
            :gsub("[Xx]2[Rr]", "Phuoc")
            :gsub("[Rr][Ee][Aa][Ll]_[Rr][Ee][Dd][Zz]", "X-crzay hub")
    end
end

local CoreGui = game:GetService("CoreGui")

for _, obj in ipairs(CoreGui:GetDescendants()) do
    pcall(UpdateText, obj)

    if obj:IsA("TextLabel") or obj:IsA("TextButton") then
        obj:GetPropertyChangedSignal("Text"):Connect(function()
            pcall(UpdateText, obj)
        end)
    end
end

CoreGui.DescendantAdded:Connect(function(obj)
    pcall(UpdateText, obj)

    if obj:IsA("TextLabel") or obj:IsA("TextButton") then
        obj:GetPropertyChangedSignal("Text"):Connect(function()
            pcall(UpdateText, obj)
        end)
    end
end)

local PlaceId = game.PlaceId

local BloxFruitsIDs = {
    [2753915549] = true,
    [85211729168715] = true,
    [4442272183] = true,
    [79091703265657] = true,
    [7449423635] = true,
    [100117331123089] = true
}

if BloxFruitsIDs[PlaceId] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/realredz999/NewRedz/main/BloxFruits.lua"))()
else
    warn("Unsupported Game PlaceId: " .. tostring(PlaceId))
end
